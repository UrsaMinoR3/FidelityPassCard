# Copy to .env for local testing. In Render, set these as dashboard env vars instead — never commit a real .env.

DATABASE_URL=postgresql://user:password@host/dbname
JWT_SECRET=change-this-to-a-long-random-string
